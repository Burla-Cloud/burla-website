import { SparseGalaxy } from "../components/SparseGalaxy";

// Default export so the docs landing can lazy-load three.js on its own chunk.
export default function DocsGalaxy() {
  return <SparseGalaxy />;
}
